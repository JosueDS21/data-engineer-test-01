# Optional: run ETL pipeline. Requires SQL Server reachable at DB_HOST.
# Copy .env.example to .env and set DB_* before: docker-compose run --rm etl
services:
  etl:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./output:/app/output
      - ./logs:/app/logs
